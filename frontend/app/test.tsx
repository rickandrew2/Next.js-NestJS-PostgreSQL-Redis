// This won't work as a route!
export default function TestComponent() {
  return (
    <div>
      <h1>This is just a component, not a route</h1>
      <p>You can't visit /test to see this</p>
    </div>
  );
}
